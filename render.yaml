services:
  - type: web
    name: omegame
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node --import tsx server.ts
    envVars:
      - key: NODE_ENV
        value: production
